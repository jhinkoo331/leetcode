#include "model\ListNode.h"
#include <vector>
#include <unordered_set>
using std::vector;
using std::unordered_set;
using std::


class Solution {
public:
	int numComponents(ListNode* head, vector<int>& G) {
		
	}
private:
	int numComponents_1(ListNode* head, vector<int>& G){
		unordered_set<int> us;
		for(int e: G)
			us.insert(e);
		unordered_set<
	}
};